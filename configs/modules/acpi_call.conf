# ACPI Call module configuration
# Required for bbswitch and ASUS hardware control

# Load acpi_call module at boot
# This module allows userspace programs to call ACPI methods
# Required for bbswitch NVIDIA GPU power control and ASUS hardware features

# No specific options needed for acpi_call
# Module will be loaded automatically when needed by bbswitch or ASUS tools